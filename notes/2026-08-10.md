# 2026-08-10

## Private-fork backbone

- Started from AIPOCH Open Science v0.12.1 at
  `218d77e17a91c13f4797e943a723cf0f8e681387` with its history intact.
- Configured `origin` as `https://github.com/mdanh-bio/research-agent.git` and `upstream` as
  `https://github.com/aipoch/open-science.git`.
- Rebranded the product to Research Agent with bundle ID `bio.mdanh.research-agent`, isolated
  production/development config and data roots, and disabled the public AIPOCH updater.
- Added project rules, indexed engineering knowledge, ADRs, roadmap, threat model, upstream-sync
  instructions, source locks, and third-party notices.
- Pinned Codex `rust-v0.147.0` and OpenCode `v1.18.12`.

## Implemented foundations

- Added backend-neutral routing contracts, deterministic policy precedence, Research Max/Balanced/
  Economy profile mappings, fallback classification, Prisma routing/run/attempt/thread-link tables,
  and a transactional attempt ledger that prevents automatic replay after side effects.
- Added a constrained Codex app-server adapter for supported thread/turn operations and read-only
  side-question forks, plus pure Auto/Steer/Side-question/Replace delivery semantics.
- Added validated compute job specifications, exact single-use approval summaries, scheduler-neutral
  driver contracts, Direct SSH and Slurm foundations, secure ControlMaster path handling, and Slurm
  state/cluster parsing.
- Added a read-only Routing settings surface labeled `Foundation / not active` and an explicit route
  target to existing backend-resolver bridge. Production conversations are not routed by it yet.

## Verification

- Prisma generation and schema validation passed.
- Full Vitest suite passed: 892 files passed, 14 skipped; 12,822 tests passed, 190 skipped.
- TypeScript node and renderer checks passed.
- ESLint passed with no errors and 18 inherited warnings.
- Changed-file Prettier validation passed. A whole-tree Prettier scan still reports 36 inherited
  upstream files that were not reformatted in this change.
- Web API-map, CLI package-content, and Claude ACP patch-integrity checks passed.
- Production Electron, renderer, and web builds passed.
- Production dependency audit reports five no-fix advisories already recorded in
  `docs/threat-model.md`; no forced dependency rewrite was applied.
- Built an unpacked Apple-Silicon application with the existing ICNS fallback after staging the
  repository's SHA-256-verified micromamba 2.8.1 payload. Verified arm64 Mach-O architecture,
  `bio.mdanh.research-agent`, `Research Agent` bundle/executable identity, bundled Prisma resources,
  micromamba execution, and strict deep ad-hoc signature validation.
- Live packaged smoke passed with an isolated temporary user-data and storage root: startup reported
  Research Agent 0.12.1 on darwin/arm64, `/api/bootstrap` returned the expected identity and isolated
  config root, and `/api/shutdown` exited cleanly without creating the default Research Agent user-data
  path.

## Remaining boundaries

- This Mac has Command Line Tools only. The configured adaptive `build/icon.icon` package path needs
  full Xcode 26 `actool`; therefore no DMG or ZIP release artifact was built or claimed. The unpacked
  validation used `build/icon.icns` only as a local packaging override.
- Routing/fallback, Codex steering, delivery modes, and parallel agent graph are foundations rather
  than active production conversation paths. Durable OpenCode steering queues remain unimplemented.
- Interactive SSH is still an interface, and the Slurm drivers are not connected to production
  submit/poll/recovery/staging/collection. No provider request, SSH login, scheduler command, or
  literature-to-HPC pilot was executed.
- No OneDrive files were read, hydrated, moved, or modified.

## Final hardening and acceptance rerun

- Converted every inherited GitHub Release, website/CDN mirror, runtime-bundle publisher, nightly
  publisher, and notarization submission workflow into a manual read-only verification path. No
  workflow retains a repository write scope, AIPOCH S3/CDN command, GitHub Release mutation,
  attestation publisher, Apple submission, or repository-secret inheritance.
- Restricted the retained runtime-bundle workflow to `osx-arm64` and a one-day private Actions
  artifact. This does not replace the application's existing public AIPOCH runtime-bundle default.
- Finished external branding for generated Specialist/Connector archives, connector and GitHub
  User-Agent headers, issue templates, contribution assets, CLI copy, and remote-compute defaults.
- Added ignore rules for Prisma's local `prisma/dev.db*` files. A final hygiene scan found no
  credential, private-key, PHI, runtime-database, archive, or generated binary among tracked or
  untracked source files; the only secret-shaped test value is deliberately fake.
- The first rebuilt packaged smoke stalled in `SecItemCopyMatching`: ordinary fresh-profile startup
  called macOS Keychain only to discover that no legacy plaintext key needed migration. Settings now
  checks for a legacy `plain:` provider/NCBI reference before consulting secure storage. The focused
  migration tests, typechecks, lint, rebuild, and subsequent packaged smoke passed.
- Final Vitest result: 898 files passed and 14 skipped; 12,944 tests passed and 190 skipped; zero
  failures. ESLint completed with zero errors and 13 inherited warnings. Prisma validation and client
  generation, web API-map, CLI/private-package, workflow-policy, and Claude ACP patch checks passed.
- The Electron journey passed four macOS cases and skipped two Windows-only cases. The final
  Apple-Silicon unpacked app reported `Research Agent` 0.12.1 and the disposable config root from
  `/api/bootstrap`, then exited cleanly through `/api/shutdown`; arm64 identity, micromamba 2.8.1,
  packaged Prisma/notices, and the strict deep ad-hoc signature were rechecked.
- A DMG/ZIP remains unclaimed because this Mac lacks full Xcode 26 `actool`. Windows packaging remains
  deferred because inherited installer/cache-cleanup paths still target Open Science state. The
  AIPOCH runtime-bundle manifest trust must also be replaced or explicitly accepted before a personal
  release.
