# 2026-08-13

## M2 Stages 2-5 final checklist verification

- Verification source: detached worktree at committed M2 tip
  `f3c095aa673224acae5dc02fbf1ee540847a93c8`; the separate dirty Stage 6 worktree was excluded.
- Runtime: Node `v22.23.2`, npm `10.9.8`, Vitest `4.1.10`, Prisma `6.19.3`, and managed Codex
  `0.147.0`.
- Stage 2 focused command covered Codex client/runtime, compatibility, managed runtime, notification
  projection, process transport, runtime links, and startup: 8 files and 64 tests passed.
- Stages 3-5 focused command covered delivery persistence/ownership/routing, direct Codex control,
  OpenCode queued delivery, side-question ownership, Session persistence, IPC/preload/composer,
  renderer stores/controllers, conversation graph, and renderer contracts: 20 files and 480 tests
  passed.
- `npm run test` passed cleanly: 915 files passed, 14 skipped; 13,066 tests passed, 190 skipped. The
  prior concurrent `completion-gate.execute-control.integration.test.ts` timeout did not recur.
- Prisma format/validate/generate passed with an isolated SQLite `DATABASE_URL`. `npm run typecheck`,
  `npm run check:web-api-map`, `npm run check:claude-acp-patch`, `npm run test:cli` (37/37),
  `npm run test:private-package` (10/10), `npm run build`, supported-file Prettier checks,
  `git diff --check`, and the scoped credential-pattern scan passed. `npm run lint` completed with
  zero errors and 13 inherited warnings.
- The first standalone `prisma validate` invocation failed only because `DATABASE_URL` was absent;
  it passed after binding an isolated temporary SQLite URL. The first Prettier invocation included
  `prisma/schema.prisma`, for which Prettier has no parser; Prisma formatted that schema, and the
  supported-file Prettier rerun passed.

### Bounded live Stage 2 validation

- Provider: Techopenclaw; endpoint `https://api.techopenclaw.com/v1`; protocol `responses`; model
  `grok-4.5`; request `b4ac82d7-0d7a-4010-a76f-1239054ac6f1`.
- Envelope: one serialized text-only request (`Reply only: OK`), zero HTTP retries, zero stream
  retries, no fallback, attachments, tools, approvals, shared-workspace writes, GLM/DeepSeek calls,
  or prompt/credential logging; isolated ephemeral read-only temporary cwd.
- Result: three streamed assistant events/four characters; SHA-256
  `e94cd381d23af2c30875257fc384d424e481454134b88dd8c5d3567a98bfdb36`; exact terminal
  `completed`; 7,568 input and 38 output tokens; zero tools, approvals, or protocol errors; clean
  thread/link/process/temp-cwd teardown in 27,142 ms. The saved active model remained
  `gpt-5.6-sol`.

### Verification boundary

- Stages 3-5 are deterministically accepted. This run did not perform a live OpenCode queued-steer
  or side-question journey, a packaged-app M2 journey, remote compute, SSH, Slurm, release packaging,
  or public upload. Stages 6-10 remain outside this final checklist and M2 overall is not complete.
