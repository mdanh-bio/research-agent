name: Nightly

# Private Research Agent updates are manual during Milestone 0. This inherited workflow is retained
# as an explicit build diagnostic. It has no schedule, tag trigger, or downstream publisher.
on:
  workflow_dispatch:

# Build and verification-metadata preparation run without repository write access.
permissions:
  actions: read
  contents: read

concurrency:
  group: nightly-build
  cancel-in-progress: true

jobs:
  plan:
    name: Check for unpublished main changes
    runs-on: ubuntu-latest
    outputs:
      should_build: ${{ steps.decide.outputs.should_build }}
    steps:
      - name: Compare main with the rolling nightly tag
        id: decide
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          published=$(gh api "repos/$GITHUB_REPOSITORY/commits/nightly" --jq .sha 2>/dev/null || true)
          if [ "$GITHUB_EVENT_NAME" = "workflow_dispatch" ] || [ "$published" != "$GITHUB_SHA" ]; then
            echo "should_build=true" >> "$GITHUB_OUTPUT"
          else
            echo "should_build=false" >> "$GITHUB_OUTPUT"
            echo "Nightly already points at $GITHUB_SHA; there is no new batch to build."
          fi

  build:
    needs: plan
    if: needs.plan.outputs.should_build == 'true'
    uses: ./.github/workflows/build.yml
    with:
      nightly: true

  # Aggregate and checksum on the low-privilege build run. A manual Nightly stops here. The separate
  # nightly artifact workflow can read these files only when an operator supplies this run's ID.
  prepare:
    name: Prepare nightly verification metadata
    needs: [plan, build]
    if: needs.build.result == 'success'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Download all build artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          path: artifacts
          merge-multiple: true

      - name: Aggregate release certification evidence
        run: >-
          node scripts/ci/release-certification-evidence.mjs aggregate
          --directory artifacts
          --output artifacts/RELEASE-CERTIFICATION.json
          --expected-sha "$GITHUB_SHA"

      - name: Generate checksums
        working-directory: artifacts
        run: |
          shopt -s nullglob
          files=(*.dmg *.zip *.exe *.AppImage *.deb)
          if [ ${#files[@]} -gt 0 ]; then
            sha256sum "${files[@]}" > SHA256SUMS.txt
            cat SHA256SUMS.txt
          fi

      - name: Upload prepared nightly metadata
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: nightly-ready
          retention-days: 1
          path: |
            artifacts/SHA256SUMS.txt
            artifacts/RELEASE-CERTIFICATION.json
          if-no-files-found: error
